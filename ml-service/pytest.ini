[pytest]
; ml-service's modules (app.py, detection.py, search.py, config.py) are flat,
; not a package — this makes ml-service/ itself importable from tests/
; without needing an __init__.py anywhere.
pythonpath = .
testpaths = tests
